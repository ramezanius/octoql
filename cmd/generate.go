package cmd

import (
	"github.com/urfave/cli/v2"
)

// Generate Generates new code based on the changes
// detected to your schema.
func Generate(*cli.Context) (err error) {
	panic("implement me")
}
